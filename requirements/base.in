# For explanations / licenses check the documentation (docs/django_packages.md)

# Django related
Django<1.11                 # Basics and boilerplate stuff
django-environ              # Configuration by environment variables according to 12Factor model
psycopg2                    # Database connector
whitenoise                  # Static file serving
sendgrid                    # just for testing, we should use django-anymail to be more flexible
django-allauth==0.30.0      # For user registration, either via email or Open ID Providers
django-crispy-forms         # Better form handling
celery                      # Asynchronous tasks
django-anymail              # Email handling
raven                       # Error handling
django-premailer            # Inline CSS in HTML email templates
django-extensions           # Django extensions (should be in local.txt after done with testing)
djangorestframework         # RESTful API
django-rest-swagger         # Documentation for the API
django-cors-headers         # Adds CORS (Cross-Origin Resource Sharing) headers to responses
django-rest-auth            # Authentication with the django-rest-framework
djangorestframework-jwt     # JSON Web token support
argon2-cffi                 # password hashing
WeasyPrint                  # HTML to PDF conversion
django-redis                # Redis handling
django-tinymce              # HTMLField for models
html2text                   # Convert HTML to Markdown

#### Still for testing ####
channels
django-channels-panel
Twisted
h2
asgi-redis
django-activity-stream
