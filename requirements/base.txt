# For explanations / licenses check the documentation (docs/django_packages.md)

# Django related
Django==1.10.5                  # Basics and boilerplate stuff
django-environ==0.4.1           # Configuration by environment variables according to 12Factor model
psycopg2==2.6.2                 # Database connector
whitenoise==3.2.3               # Static file serving
sendgrid==3.6.3                 # just for testing, we should use django-anymail to be more flexible
django-allauth==0.30.0          # For user registration, either via email or Open ID Providers
django-crispy-forms==1.6.1      # Better form handling
celery==4.0.2                   # Asynchronous tasks
django-anymail==0.7             # Email handling
raven==6.0.0			        # Error handling
django-extensions==1.7.5        # Django extensions (should be in local.txt after done with testing)
djangorestframework==3.5.4      # RESTful API
django-oauth-toolkit==0.11.0    # OAuth 2.0 support